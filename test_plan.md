1. **Fix Missing Battle Buttons**: The `isActionRunning` or similar flags could be an issue, but looking closely at `updateBattleUI` and the `document.getElementById` override, there might be a problem with the display property of the `battleControls` or `battleControls_p2` not being handled correctly or some action block. I need to make sure that the battle buttons show correctly. Looking at `startNpcBattle`, we see `document.getElementById('battleControls').style.display = 'grid';` but perhaps there's a problem when it hides it, or the activeBattlePlayer isn't getting correctly updated or the `p2` controls aren't shown. Wait, `document.getElementById('battleControls')` will target `battleControls_p2` if it's player 2 thanks to the override. Let me double check if `battleControls_p2` has the buttons. Wait! In `startNpcBattle`, we have `window.activeBattlePlayer = playerNum;` then `document.getElementById('battleControls').style.display = 'grid';`. If it's p2, this should show `battleControls_p2`. Wait, wait, looking at `window.updateBattleUI()`, there's no code that explicitly shows `battleControls`. Oh wait, I see `const ctrlP1 = document.getElementById('battleControls'); if (ctrlP1) ctrlP1.style.display = 'none';` inside `window.handleFaint` maybe?
Let's figure out why battle buttons are missing. I'll read where they are defined and the flow of `startNpcBattle`. Wait! The user says "After players accept the trainer battle invitation there's no battle buttons for the player to be able to attack, swap, or flee". Let's trace `startNpcBattle` again.

2. **NPC/Wild Creature Swimming**: Apply the swimming animation logic (tint, alpha, crop, bobbing) to NPC trainers and wild creatures by checking if they are on a water tile (`obs` in mapData) during their update loop (in `update()`). Currently, wild creatures only move to grass: `if (mapData[row] && mapData[row][col] === 'grass' ...)`. I need to change this so they can also move to `obs` (water), and if they are on `obs`, apply the swimming animation. Same for `window.npcTrainersGroup` children.
