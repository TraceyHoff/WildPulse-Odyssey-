diff --git a/index.html b/index.html
index 5be0746..7132a8f 100644
--- a/index.html
+++ b/index.html
@@ -30155,32 +30155,6 @@ function generateTileTextures(scene) {
                 if (scene && scene.textures) {
                     if (scene.textures.exists(tileKey + '_tile')) scene.textures.remove(tileKey + '_tile');
                     window.finalizeFurnitureTexture(scene, tileKey + '_tile', canvas);
-
-                    if (window.miniTilesGroup) {
-                        window.miniTilesGroup.getChildren().forEach(sprite => {
-                            if (sprite.miniTileType === item || sprite.miniTileType === tileKey || (sprite.texture && sprite.texture.key.startsWith(tileKey + '_tile'))) {
-                                let textureToSet = tileKey + '_tile';
-                                if (sprite.texture.key.includes('_90')) textureToSet += '_90';
-                                else if (sprite.texture.key.includes('_180')) textureToSet += '_180';
-                                else if (sprite.texture.key.includes('_270')) textureToSet += '_270';
-                                sprite.setTexture(textureToSet);
-                            }
-                        });
-                    }
-                    if (window.p1MiniTilePreviewSprite && window.p1MiniTilePlacementMode === item) {
-                        let texToSet = tileKey + '_tile';
-                        if (window.p1MiniTilePreviewSprite.texture.key.includes('_90')) texToSet += '_90';
-                        else if (window.p1MiniTilePreviewSprite.texture.key.includes('_180')) texToSet += '_180';
-                        else if (window.p1MiniTilePreviewSprite.texture.key.includes('_270')) texToSet += '_270';
-                        window.p1MiniTilePreviewSprite.setTexture(texToSet);
-                    }
-                    if (window.p2MiniTilePreviewSprite && window.p2MiniTilePlacementMode === item) {
-                        let texToSet = tileKey + '_tile';
-                        if (window.p2MiniTilePreviewSprite.texture.key.includes('_90')) texToSet += '_90';
-                        else if (window.p2MiniTilePreviewSprite.texture.key.includes('_180')) texToSet += '_180';
-                        else if (window.p2MiniTilePreviewSprite.texture.key.includes('_270')) texToSet += '_270';
-                        window.p2MiniTilePreviewSprite.setTexture(texToSet);
-                    }
                 }
             };
             img.src = svgDataUrl;
@@ -31882,8 +31856,10 @@ window.spawnMiniTiles = function(scene) {

                 if (tile.type.includes('carpet')) {
                     mSprite.setDepth(8.5);
+                } else if (window.smallItems && window.smallItems.includes(tile.type)) {
+                    mSprite.setDepth(9.5);
                 } else {
-                    mSprite.setDepth(10 + (tile.y / 10000));
+                    mSprite.setDepth(9);
                 }
                 mSprite.setScale(window.getRealisticScale(tile.type));
                 mSprite.isMiniTile = true;
@@ -32303,8 +32279,6 @@ window.isValidMiniTileLocation = function(x, y, playerNum, itemName) {
     if (!mapData || !mapData[row] || col < 0 || col >= mapData[0].length) return false;

     const tileType = mapData[row][col];
-    if (tileType === 'home_wall' || tileType === 'home_holo_wall' || tileType === 'water' || tileType === 'tree') return false;
-
     const isInside = window.isPlayerInsideHome && window.isPlayerInsideHome(playerNum);
     const isInsideOtherHome = window.isPlayerInsideOtherPlayerHome && window.isPlayerInsideOtherPlayerHome(playerNum);
