--- index.html
+++ index.html
@@ -4092,6 +4092,7 @@

     window.activeRandomEvent = chosen;
     window.activeRandomEventEndTime = window.totalElapsedMs + duration;
+    window.activeRandomEventStartTime = window.totalElapsedMs;

     localStorage.setItem('wildpulse_active_random_event', chosen);
     localStorage.setItem('wildpulse_active_random_event_end_time', window.activeRandomEventEndTime.toString());
@@ -4145,6 +4146,7 @@
         const targetEvent = isRainWeather ? 'Rain' : (isStormWeather ? 'Thunderstorm' : 'Flash Freeze');
         if (window.activeWeatherEvent !== targetEvent) {
             window.activeWeatherEvent = targetEvent;
+            window.activeWeatherEventStartTime = window.totalElapsedMs;
             localStorage.setItem('wildpulse_active_weather_event', targetEvent);

             if (window.showModernNotification) {
@@ -4166,6 +4168,7 @@
     } else {
         if (window.activeWeatherEvent && window.activeWeatherEvent !== 'None') {
             window.activeWeatherEvent = 'None';
+            window.activeWeatherEventStartTime = 0;
             localStorage.setItem('wildpulse_active_weather_event', 'None');
             if (window.showModernNotification) {
                 window.showModernNotification("🍃 The weather event has subsided. The world returns to normal.", "#4caf50");
@@ -4283,12 +4286,41 @@
         return;
     }

-    el.innerHTML = parts.join(' | ');
-    const finalColor = colors[0] || "#00ffcc";
-    el.style.color = finalColor;
-    el.style.borderColor = finalColor;
-    el.style.boxShadow = `0 0 10px ${finalColor}80`;
-    el.style.display = 'flex';
+    let showBadge = false;
+    if (hasRandomEvent) {
+        const startTime = window.activeRandomEventStartTime || (window.activeRandomEventEndTime - 240000);
+        if (window.totalElapsedMs < startTime + 6000) showBadge = true;
+    }
+    if (hasWeatherEvent) {
+        const startTime = window.activeWeatherEventStartTime || 0;
+        if (window.totalElapsedMs < startTime + 6000) showBadge = true;
+    }
+
+    if (!showBadge) {
+        el.style.display = 'none';
+    } else {
+        el.innerHTML = parts.join(' | ');
+        const finalColor = colors[0] || "#00ffcc";
+        el.style.color = finalColor;
+        el.style.borderColor = finalColor;
+        el.style.boxShadow = `0 0 10px ${finalColor}80`;
+        el.style.display = 'flex';
+    }
+
+    const container = document.getElementById('eventBubblesContainer');
+    if (container) {
+        const bubbles = Array.from(container.children);
+        bubbles.forEach(bubble => {
+            const source = bubble.dataset.source;
+            const color = bubble.dataset.color || '#00ffff';
+            let pct = 100;
+            if (source === 'random') {
+                const remainingMs = window.activeRandomEventEndTime - window.totalElapsedMs;
+                pct = Math.max(0, Math.min(100, (remainingMs / 240000) * 100));
+            }
+            bubble.style.border = '2px solid transparent';
+            bubble.style.background = `linear-gradient(rgba(0, 20, 40, 0.9), rgba(0, 20, 40, 0.9)) padding-box, conic-gradient(${color} ${pct}%, rgba(255,255,255,0.1) 0) border-box`;
+        });
+    }
 };

 window.windAngle = 0;
@@ -7999,13 +8031,9 @@
                 bubble.className = 'event-bubble';
                 bubble.innerHTML = emoji;
                 bubble.dataset.source = source;
-                if (colorOverride) {
-                    bubble.style.borderColor = colorOverride;
-                    bubble.style.boxShadow = `0 0 10px ${colorOverride}`;
-                } else {
-                    bubble.style.borderColor = '#00ffff';
-                    bubble.style.boxShadow = '0 0 10px rgba(0,255,255,0.5)';
-                }
+                const finalColor = colorOverride ? colorOverride : '#00ffff';
+                bubble.dataset.color = finalColor;
+                bubble.style.boxShadow = `0 0 10px ${finalColor}`;

                 const container = document.getElementById('eventBubblesContainer');
                 if (container) {
