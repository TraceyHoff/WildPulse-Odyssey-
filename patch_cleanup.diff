<<<<<<< SEARCH
    if (currentPlayer.battleStats && currentPlayer.battleStats.skipTurn) {
        currentPlayer.battleStats.skipTurn = false;
        logBattle(`<span style="color:#ffd700; font-weight:bold;">${currentPlayer.nickname || currentPlayer.name} is stunned and couldn't move!</span>`);
        updateBattleUI();
        if (callback) setTimeout(callback, 300);
        return;
    }

    if (action === 'attack') {
=======
    if (action === 'attack') {
>>>>>>> REPLACE
