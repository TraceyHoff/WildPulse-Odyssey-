<<<<<<< SEARCH
        let accuracy = 1.0 * (currentPlayer.battleStats ? currentPlayer.battleStats.accuracyMod : 1.0);
        let enemyEvasion = currentEnemy.battleStats ? currentEnemy.battleStats.accuracyMod : 1.0;
        let hitChance = Math.max(0.1, Math.min(1.0, 0.95 * (accuracy / enemyEvasion)));
=======
        let accuracy = 1.0 * (currentPlayer.battleStats ? currentPlayer.battleStats.accuracyMod : 1.0);
        if (currentPlayer.battleStats && currentPlayer.battleStats.accuracyDecrease) {
            accuracy *= 0.5;
            currentPlayer.battleStats.accuracyDecrease = false;
        }

        let enemyEvasion = currentEnemy.battleStats ? currentEnemy.battleStats.accuracyMod : 1.0;
        let hitChance = Math.max(0.1, Math.min(1.0, 0.95 * (accuracy / enemyEvasion)));
>>>>>>> REPLACE
